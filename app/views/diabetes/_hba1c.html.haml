.row
  .col-md-4
    Hba1c
  .col-md-5
    - if @client_hba1c.last.present? 
      = @client_hba1c.last.mmol 
    - else 
      N/A
    MMOL/l
    -  if @client_hba1c.last.present?
      = @client_hba1c.last.activity_date.strftime("%d-%m-%Y")
    - else
      Laatste meetmoment is onbekend
  .col-md-3

.row
.col-md-12
.row
  .col-md-4
    Streefwaarde
    = @client_hba1c_target.mmol
  .col-md-5
    - if @client_hba1c.first.present? 
      = @client_hba1c.first.mmol 
    - else 
      N/A
    MMOL/l
    -  if @client_hba1c.first.present?
      = @client_hba1c.first.activity_date.strftime("%d-%m-%Y")
    - else
      Laatste meetmoment is onbekend
  .col-md-3
    - if @client_hba1c.first.mmol <= 53 
      Goed
    - elsif @client_hba1c.first.mmol > 53 && @client_hba1c.first.mmol < 69
      Aanvaardbaar
    - elsif @client_hba1c.first.mmol > 59
      slecht