.co-bg
.row
  .col-md-3
    .instelling-image
  .col-md-8
    .search
      %form
        %input.SearchField{:placeholder => "Search...", :required => "", :type => "text"}
          %input.SearchIcon{:type => "image", :value => "Search"}
  .col-md-1
    // search nog niet werked helaas
.row
  .col-md-12
    = link_to "Client toevoegen", new_client_path, class: "btn btn-primary pull-right"
.row
  - @client.each do |c|
    .col-md-6
      .client-container
        .col-md-2
          .client-photo
          // {:style => "background-image:url(client-replacement.jpg)"} add the photo with inline haml!
        .col-md-9
          %p.txt-clinetenoverzicht-h1
            = c.name
            = c.surname
          %p.txt-clinetenoverzicht-h2= c.date_of_birth
        .col-md-1
          = link_to client_path(c) do
            %div.arrow-next